services:
  - type: web
    name: jobfinder-python-scraper
    env: python
    rootDir: backend/scraper-service-py
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free

